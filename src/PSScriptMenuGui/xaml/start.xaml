<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" Icon="{Binding IconPath}"
        Height="Auto" Width="Auto" MaxHeight="800" MinHeight="200" MaxWidth="600"
        SizeToContent="WidthAndHeight" WindowStartupLocation="CenterScreen">
    <ScrollViewer Padding="10,0,10,10" VerticalScrollBarVisibility="Auto">
        <ItemsControl ItemsSource="{Binding MenuItems}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                    </Grid>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" 
                                Content="{Binding ButtonText}"
                                Background="{Binding BackgroundColor}"
                                Foreground="{Binding ForegroundColor}"
                                MinHeight="50" VerticalAlignment="Top" Padding="10" Margin="0,5,0,5"
                                Tag="{Binding Reference}"/>
                        <TextBlock Grid.Column="1" 
                                   Text="{Binding Description}"
                                   TextWrapping="Wrap" Padding="10,5,0,5" VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</Window>